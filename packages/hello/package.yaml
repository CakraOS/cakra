name: hello
version: 0.1.0
release: 1
architecture: aarch64

source: src

build:
  - cc hello.c -o hello

install:
  - echo "${DESTDIR}"
  - mkdir -p "${DESTDIR}/usr/bin"
  - cp hello "${DESTDIR}/usr/bin/hello"
